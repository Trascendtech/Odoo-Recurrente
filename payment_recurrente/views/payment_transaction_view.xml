<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="payment_transaction_form" model="ir.ui.view">
        <field name="name">Recurrente Transaction Form</field>
        <field name="model">payment.transaction</field>
        <field name="inherit_id" ref="payment.payment_transaction_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="action_check_payment_status"
                        string="Check Payment Status"
                        type="object"
                        invisible="provider_code != 'recurrente'"
                        class="btn-secondary"/>
            </xpath>
            <field name="provider_reference" position="after">
                <field name="id_recurrente_checkout"
                       readonly="1"
                       invisible="provider_code != 'recurrente'"
                       groups="base.group_no_one"/>
                <field name="url_recurrente_checkout"
                       readonly="1"
                       invisible="provider_code != 'recurrente'"
                       groups="base.group_no_one"/>
                <field name="product_recurrente_checkout"
                       readonly="1"
                       invisible="provider_code != 'recurrente'"
                       groups="base.group_no_one"/>
            </field>
            <xpath expr="//sheet" position="inside">
                <notebook>
                    <page name="failure_reasons" string="Failure Reasons">
                        <field name="payment_intent_failure_reasons" readonly="True"/>
                    </page>
                </notebook>
            </xpath>
        </field>
    </record>

</odoo>
